# Audit boundary

The verifier returned `PASS` with an empty error list.  The direct Lean file
and its separate axiom-audit file both compiled with
`-DwarningAsError=true`.  The printed dependency union is contained in
`Classical.choice`, `Quot.sound`, and `propext`; there is no `sorryAx`.

The numerical evidence proves only the displayed finite identities.

The Python and C++ exhaustive searches agree on every aggregate result, but
the 43-million-case loop is not yet replayed inside Lean.  Lean checks the
reported singleton and its logical boundary.  A compact kernel-checkable
enumeration certificate remains an open formalization task.

* `F_7(2)=13^2` retires the claim that a simple polynomial root forces an
  exponent-one integer specialization.  Since `A_7=239` is simple, it is not
  a full squarefull packet.
* The parameter `282` retires the moving-parameter opposite-repeat exclusion.
  Its Pell coefficient is `19882`, and each channel still has simple factors,
  so it does not refute the fixed coefficient-two Pell target.
* Nonzero exit digits for three primes certify their exact depth-two status;
  the finite list is not an unbounded depth-three theorem.
* The `T<=20,000,000` index-three enumeration is exhaustive only in its
  stated interval.  Its sole whole-`F`-channel squarefull value has an
  opposite channel with five exponent-one factors; the zero full-hit count
  is not extrapolated.

Accordingly, the fixed balancing-Pell route and both proof/disproof searches
remain active.
