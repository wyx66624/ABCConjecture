# Pell factor-quotient/projective coupling evidence

This bundle certifies the computation in
`research/ABC_PELL_LUCAS_FACTOR_QUOTIENT_PROJECTIVE_COUPLING_2026_09_02.md`.

It contains two independent implementations of Pell powering and of the
Lucas coefficients.  The producer uses the product/factorial coefficient
formula and direct elementary-symmetric sums.  The verifier uses the closed
binomial formula and a recursive elementary-symmetric update.

The exact scope is:

* 57 actual odd-prime endpoint-curvature identities through index 271;
* 57 explicit exponent-one prime witnesses, so no actual squarefull packet
  occurs in that finite range;
* 13 fully factored two-channel quotient/character ledgers through index 43;
* the complete local-only L3 counterexample `(ell,q,r)=(3,7,797)`; and
* the actual index-seven counterexample to endpoint `U^3` divisibility.

A bounded exclusion is never interpreted as an unbounded theorem.

