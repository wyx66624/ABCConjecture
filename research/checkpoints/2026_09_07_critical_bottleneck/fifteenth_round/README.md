# Fifteenth continuation: unit-normalized signed moments

This directory contains subsequent research only. All earlier published
rounds and the historical fourteenth-round next-note bytes are preserved.

The ordinary US1--US4 proof first normalizes the actual three arm phases
at a fixed prime. Signed products then inject into an n-torsion target
using an actual Gram determinant bound. The full signed-ball count gives
a single high-depth owner, a complete 5/2 per-prime positive-excess
budget, a 5/B normalized budget, and two different singleton-support
thresholds. These are actual fixed-prime statements on an independent
root domain. They do not bound the number or total weight of distinct
far prime labels, or close the global signed ABC tail.

Files:

- `unit_signed_moments.md`: complete ordinary proof; root and both peers PASS.
- `ordinary_signed_scope.md`: ordinary proof of the selected finite
  formal scope, also root and both peers PASS before implementation.
- `Lean/SignedMomentArithmetic.lean`: 22 completed finite theorems.
- `formal_completion.md`: exact theorem inventory, hypotheses and limits.
- `verify_signed.py`: pinned bounded fresh compiler and complete axiom audit.
- `verification/signed_validation.json` and `signed-lean.log`: actual
  fresh PASS for 22 new and 27 unchanged dependency declarations.
- `paper/unit_signed_moments.tex`: complete paper transcription; both
  peers read the full final text and passed transcription review.
- `review.md`: independent reviews and explicit evidence distinctions.
- `seal_sources.py` and `source_evidence.json`: exact eleven-file
  delivery whitelist and hash seal, excluding all sixteenth-round work.

The new source SHA-256 is
`01d03813915c7fcffcf2994cfde948a0d961ac5b6dcc69af77fb05beb5c65327`.
Both peers read all final signatures and proofs and passed the semantic
scope review. They do not claim an additional compiler execution.

The root checkpoint separately owns the actual Gram and signed-product
finite constructions. The independent geometric continuation proves
exact elliptic ranks by ordinary descent while preserving the separate
problem of all points satisfying the simultaneous quartic gate.
