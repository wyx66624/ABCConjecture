# Final eleventh-continuation integration review

Standard ABC is still unproved and undisproved. The main research goal
remains active; publication of this reviewed partial checkpoint is not
completion of the conjecture.

The root fully reviewed PC, IL, CI, CR, LC, SQ, GE and BK ordinary proofs
and their final TeX transcriptions. Each author also obtained independent
agent reviews, recorded in the three `eleventh_round` directories. Root
formal proofs were preceded by ordinary proofs and independently read at
the level of complete Lean statements and proof bodies. Those source
reviews are distinguished from actual compiler reruns.

`verification/mathlib_validation.json` records the successful fresh
31-new/53-old run, all seven source hashes, 84 exact axiom queries,
compiler version and pinned Mathlib revision. Its complete output is
`verification/fresh-mathlib-build.log`. The local reflected depth law,
prime lower bound and global height/tail/small-mass conditions are
explicit, with no extra mathematical axioms.

`verification/finite_replay_validation.json` records the root's independent
execution of both exact replays, including unchanged source and canonical
certificate hashes. The Gaussian-cover replay uses exact polynomial
arithmetic and complete trial division on its bounded examples. It is not
a rational-point completeness computation.

The manuscript was compiled three times with 176 stable actual TeX inputs,
zero overfull boxes and zero unresolved or duplicate references. One LC
source control character was corrected to the intended fraction; three
long module names gained break opportunities. Final transcriptions and
their revised hashes were checked after these layout-only corrections.

The designated PDF has 486 pages and SHA-256
`a218aa002ee3ffdbcef99d13b447a69ddb8b213b374e2bc37c1028a91b748021`.
The seal checks all 164 predecessor child TeX sources unchanged and the
first 463 pages' extracted text identical to the previous 469-page PDF.
Root actually viewed pages 1, 464 and 480--486; adversarial viewed
465--472; independent viewed 473--479. All 24 inspected pages passed.
The seal binds the actual PNG hashes and independent review records to
the PDF. It does not claim visual inspection of every earlier page.

The root staged scope consists only of these four eleventh checkpoint
directories, the new targeted workflow, the designated PDF, master TeX
and four mutable entrypoint documents. Original user changes and
twelfth-round candidates are excluded. Earlier published checkpoint
sources remain frozen. New research continues in separate twelfth-round
directories, with the far signed tail and the distinct geometric point
conditions still open.
