# Fourteenth continuation: independent ordinary review

This is subsequent research, separate from the thirteenth manuscript batch.
That batch was published without rewriting main as
d2993220fc7ecf068905acbfc63ad7daaf562e50, with local HEAD, origin/main
and remote main independently verified equal. Its PDF has 513 pages,
3,334,366 bytes, SHA-256
fefb61573ea9408f26f5c7d85f5383899bd9f29e8175033cc1f86998a993a74a.
The seal records 191 actual inputs, 184 byte-identical predecessor child
sources, 497 unchanged text-prefix pages and 17 actually viewed rasters.
All 37 new declarations passed fresh compilation and full axiom audit.
The four GitHub jobs were started and were not yet complete at publication.
The dedicated run 34113328294 subsequently succeeded: all 37 declarations
and queries completed at 2026-09-07T10:50:54Z, the exact replay at
10:51:24Z, and the job at 10:51:28Z. Shared scoped run 34113328236 also
passed. The broader kernel and all-module jobs were still running at
that observation; their later outcome is not inferred here.
ABC remains unproved and undisproved. The parent signed-tail, exceptional
root, smooth-support and surviving geometric unit routes all remain live.

## AP1--AP4: complete ordinary proof, root PASS

Root read the entire next_adaptive_precision.md before new formal work.
For an actual block root the norm is below 36B^2, and each of the three
boundary coordinates is at most 2/sqrt(3) times the complex absolute value.
Thus c<(8B)^n. At q>8B exactly one primitive boundary coordinate is
divisible, giving e_q logq<=t<n log(8B). The rank-one case is impossible
even at first depth because q exceeds both original nontrivial factors.

The UM determinant is strictly below (8B)^(2nu), so precision e and
moment nu may be separated whenever q^e is at least this threshold.
Actual norm denominators stay units and the finite torsion group remains
of size at most 3n. Independence therefore gives the same multiset bound
at each chosen precision. With h=max(4,ceil(2rL/logq)), r=ceil(sqrt n),
the low count is sqrt(6n) and the high count at most three.

If h=4, the low range is empty. Otherwise (h-4)logq<=2rL absorbs the
rounding, without requiring an upper bound for logq. Paying every low
layer costs at most 10nL, and the three high roots cost at most 3nL.
This proves 13nL and normalized 26/B for every q>8B. The two-class sieve
gives 52/(n-1), bounded by 55/n for n>324, with the same logarithmic
denominator. Counting only the actual finite far-depth prime support
gives 26 times its cardinality divided by B. The small-cardinality
hypothesis has not been proved, and is not equivalent to the signed target.

Critical is the author; adversarial and independent also reported complete
ordinary PASS before the proposed finite formalization. Source copies and
formal scope will be recorded in their fourteenth-round locations.

## OC1--OC3: complete ordinary proof, root PASS

Root read the complete next_two_owner_concentration.md. The moments two,
three and r=ceil(sqrt(6n)) give low count sqrt(6n), middle count below
3 n^(1/3), and high count at most two. The latter uses the strict inequality
choose(r+2,2)>r^2/2>=3n. Precision thresholds h0 and h1 include their
maxima with four; all possible empty middle intervals are included.

The min(2,|I|) largest actual depths contain every depth at least h1,
even with ties. Outside this set, all remaining layers cost at most
15 sqrt(n)L plus 24 n^(5/6)L, hence 39 n^(5/6)L. The owners contribute
at most 2nL by the actual single-coordinate cap. Normalization gives
78 n^(-1/6)/B outside the two owners. No unrestricted sum over primes
is made in this estimate.

When logq>=(r/2)L both precision thresholds equal four, so each actual
prime with positive excess supports at most two indices. This yields an
actual incidence graph with allowed single-vertex edges and repeated
prime-labeled edges. It bounds neither the number of labels nor total
owner cost. The proof explicitly retains the prime-dependent choice of
owners and all negative signed credit omitted by the positive graph.
Both peer reviewers also gave complete ordinary PASS.

## DG1--DG4: complete ordinary proof, root PASS

Root read the entire next_private_density_gain.md and actually opened
Kevin Ford's author-hosted Sieve Methods Lecture Notes, Spring 2023,
https://ford126.web.illinois.edu/sieve2023.pdf. Theorem 4.1, printed
page 45, and the preceding page explicitly give the Selberg bound with
|lambda_d|<=1 and the double-remainder sum. Taking D=z^2 gives the exact
finite form used in the note.

After extracting a cofactor m and a root b modulo m, Q(b+mt)/m is an
integer quadratic of discriminant -27. Its local root count drops from
two to one at primes dividing m, with no new fixed divisor. Counts in
the actual t interval have remainder at most the local root count.

The base Selberg denominator is at least c logz: the fixed-modulus prime
estimate gives a finite Euler product of mass comparable to logy, and
its mean log-divisor at y=z^(1/4) is at most (logz)/2. Weighted Markov
keeps half the mass below z. This is a finite auxiliary weight measure,
not an assumption on the distribution of actual roots. Splitting a
squarefree divisor into its m-supported and coprime parts gives the
exact uniform comparison J0(z)<=A(m)Jm(z).

The coarse double error is at most z^4. Summing all cofactors, including
prime powers, uses the exact Euler factor 1+2p/((p-2)(p-1)); its product
is O(log(3X)). With X=49B^epsilon and z=B^(1/10), the complete summed
remainder is O(B^(3/5)), uniformly for 0<epsilon<=1/10. The main term
is C0 epsilon B+O(B/logB). No moving quadratic prime-value asymptotic
or unproved smooth-number distribution is used.

The existing private norm prime above 12B is unique within a root and
across the block, and simple. The original full-block prime mass is
therefore bounded above by (2-epsilon)|S|logB plus epsilon times the
specific extreme-prime exception count, and O(B). Choosing C0 epsilon
at most 1/4 gives liminf density strictly above one half. The proposed
delta0=epsilon/(8(2-epsilon)) is positive for all sufficiently large B.
It has not been numerically optimized. This improves the actual private
domain and uses the full-block mass once; it does not iterate a mass
estimate on discarded roots. Peer final reviews and any new TeX/formal
steps are to be recorded separately. The signed far tail remains open.

## SS1--SS4: complete ordinary proof, root PASS

Root read the entire simultaneous_elliptic_gate.md. The normalized
coordinates, kappa and gamma identities eliminate to the displayed
quartic over E. Conversely, on the stated open they recover both square
coordinates at the same source s. The tower D to H1 to E has degree
four, and Z generates the full field; hence its monic quartic is
irreducible, including after extension to the algebraic closure.

The positive domain is reversible. The strict inequality 0<Z^2<R gives
Delta>108h^2, while Yh>0 gives positive v. The cases X=0, X=-2,
kappa=+/-1, s=-1 and source infinity are excluded explicitly; the
repeated discriminant gives a negative square value. The positive
domain is unramified, so normalization introduces no hidden branch
choice. The opposite Z signs retain the ordered seed directions.

For integral Weierstrass coordinates the rational C=Zd^3 satisfies
a monic integer quartic and is therefore integral. Clearing the
identity gives (B^2-C^2)^2=36d^2K^2(3B^2+C^2). Primewise comparison
proves integrality of its displayed quotient L. At p>3 dividing dK,
the two disjoint cases p|d and p|K each make B a unit. The identity
then makes C and 3B^2+C^2 units, giving the exact equality of depths
and allocation to exactly one of B-C and B+C. Primes two and three
are not included in this assertion, and no height bound is inferred.

The explicit rational F3 has degree three with no cancellation at
X=-3, and its derivative identity gives a morphism E to E'. It sends
O to O; root actually opened Milne, Elliptic Curves, Chapter II,
Proposition 1.5, printed page 49, at
https://www.jmilne.org/math/Books/EC2.pdf, together with the preceding
origin-preserving definition. Thus it is a group homomorphism. The
abscissa-degree diagram gives degree three, and the pole fibers give
the three geometric kernel points. The displayed point image and
triple of (6,9) follow from the rational addition law.

The resulting isogeny Jac(H1)~E^2 does not eliminate the common-source
quartic condition, prove rank one, or determine all rational points.
This ordinary review preceded any corresponding proposed new formal
implementation. Both peer full reviews and exact replays remain separate
records; the surviving cubic-unit point problem is still open.

## VG1--VG4: complete ordinary proof, root PASS

Root read the entire very_large_prime_incidence.md. At the very-large-prime
threshold the actual fourth-depth hit set has at most two roots by the
reviewed uniform moments. Primitivity assigns each hit to exactly one
nonzero boundary arm. For every vertex subset and every set of distinct
prime labels, the complete prime-power packet divides the actual arm
product by primewise valuation comparison. Its logarithm yields the
simultaneous mass budget, including all labels at once.

An arm with m labels satisfies 2rLm<=log b<nL, hence m<n/(2r) and
degree at most floor(n/(2r)). Singleton edges and repeated two-vertex
edges are retained. Greedy coloring uses at most 2d-1 matchings: at
each two-endpoint label at most 2(d-1) earlier labels are adjacent.
The exact positive-excess inequality keeps its three radical units.

The displayed singleton weighted example establishes only a limitation
of the finite degree/count/mass inequalities by themselves. Its labels
and equal logarithmic weights are not constructed from actual primes,
integer arms or Eisenstein roots. The proof explicitly preserves the
additional actual simultaneous divisibility, cross-prime structure and
negative signed credit as possible sources of progress. It does not
retire the arithmetic route or claim an ABC counterexample.

## Finite formal snapshots before joint compilation

Root's PrivateDensityMass has eleven declarations and passed a fresh
source build and complete standard-three axiom audit. Adversarial reviewed
the prior ten-declaration snapshot; critical read the full eleven. The
last density theorem includes the actual finite exceptional remainder R,
constant height correction c and their combined budget
loss+epsilon R/B+c/ell. It therefore does not silently discard these
terms when applying the simpler mass-cut lemmas to DG.

Root read all twelve final AdaptiveOwnerArithmetic declarations and the
ordinary scope. Source/semantic review: PASS. They choose actual top-two
indices, keep every natural truncated depth term inside its finite sum,
and derive the exact two-stage weighted budget from actual ceilings and
the explicit high multiset-count hypothesis. They stop at the displayed
finite weighted expression; the 39/78 real-power estimates remain ordinary.
Their author's fresh build and full standard-axiom queries passed with
the fifteen unchanged UM dependencies. Joint root compilation and final
source-hash reviews remain separate records.

## User-requested reuse of the official Fermat library

The user directed the group to reuse the newly open-sourced Claude Fermat
formalization where applicable and keep the main effort on diverse ABC
proof attempts. Root actually opened the official repository
https://github.com/anthropics/fermats-last-theorem. The project exports
the complete natural-number theorem and FinalCheck derives Mathlib's
FermatLastTheorem statement with the standard-three axiom guard. This is
different from the proposition definition in the older pinned Mathlib.

The official package uses Lean 4.33.1 and Mathlib 4.33.0, whereas the
published local cores use 4.32.0. The adversarial agent is checking an
isolated pinned import and useful dependencies. The official reported
full build is large and cannot be called a local successful import merely
from reading its logs. Existing verified environments remain unchanged.
Only exact applications with all Fermat hypotheses will be used; the
full signed near-power problem does not follow just from pure-power
Fermat exclusion. The other agents continue their independent mathematical
routes while the reusable-library verification proceeds.


## Final full-source and TeX review, and updated predecessor CI

The predecessor's remaining kernel run 34113328215 and all-module run
34113328266 were subsequently read as completed SUCCESS. All four runs
for d2993220fc7ecf068905acbfc63ad7daaf562e50 have now passed; this supersedes
the earlier still-running observation without changing the sealed old batch.

Root read all final twelve SS Lean declarations, including the actual
integer gcd-to-unit argument, homogeneous equations and p-adic depth law.
The joint fresh run passed all 35 new and 15 old declarations with all
50 exact standard-three axiom queries. Adversarial independently checked
all final source bytes and the full manifest/log, including all queries.

Root read all six final TeX inputs in full. The AP/OC and DG transcriptions
retain the complete ceil layers, uniform sieve errors and all open gates.
VG retains actual divisibility and limits its abstract example to finite
inequalities. SS retains the same source, all positive-chart exceptions,
ordinary geometric scope and its twelve precise formal declarations.
Both mathematical agents reviewed the root finite bridge; their requested
dK nonzero condition was explicitly added. Critical and adversarial reviewed
the final research status, including the actual S2 import and unverified
full-FLT distinction. These are internal independent reviews.

Both exact standard-library replays passed root's --check execution with
unchanged source and certificate bytes. The 80 signed multiples are only
those specified points; no all-point or basis theorem is inferred.
