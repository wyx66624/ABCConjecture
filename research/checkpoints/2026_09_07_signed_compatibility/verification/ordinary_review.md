# Ninth continuation: root independent ordinary and source review

The root read the complete SA1--SA4, CE1--CE4, AC1--AC3 and EP1--EP4
ordinary arguments, and passes their stated claims. PP1--PP5 and the
integer assembly were written as ordinary proofs before the corresponding
formal or manuscript integration. Both independent reviewers' full PP
and EP records are retained in their own ninth-round directories.

SA is an actual arm construction. Normalizing the power at residues 1
and 5 modulo 6 gives integer homogeneous quotients on all three boundary
lines. Output primitivity supplies their pairwise coprimality. The
ramified norm-three example gives three quotients -9 and proves that
deleting the unramified premise would be false. For two arms the local
identity e <= 2 + (e-2)_+ pays the required radical credit, and the third
arm retains its full negative contribution. The overlap with the old
boundary costs at most log T_1. The final SA inequality follows without
discarding the negative signed weights. Rotation into the positive sector
is stated before applying LR2; unit and conjugation permute all absolute
boundary arms. The uniform asymptotic corollary requires sufficiently
large extraction exponent and the two-arm excess condition. It does not
prove that arbitrary roots satisfy this membership condition.

CE uses the exact difference F-M^2=ab c^2. The order at every supported
q distinct from p is p, while the p-exception occurs precisely on p|D
and has depth one. The four-ninths polynomial certificate, sum lower
bound and full bad-part divisibility retain all depths. They give a
necessary full valuation mass, not a radical bound or an existence result.
The root read both final CE Lean sources and final CE TeX in full.

The root subsequently read the final SA and AC TeX in full and passes
both transfers, including the positive-sector LR2 invocation, the
sufficiently-large-n quantifier and the actual private-depth illustration.

AC combines CE with the actual first Eisenstein root. A q=1 mod p
outside 1 mod 6p must be inert. Its boundary rank divides both p and
(q+1)/3 and is therefore one. Exact homogeneous lifting puts its entire
c-depth into one small input arm. The disjoint bad-support identity and
3J^2<=4R give the stated 27/16 budget and mass exponent 1-3/p.

PP repeats the top-factor allocation at p^k and retains the exception p.
Its guarantee at fixed p tends to 1-2/p, not one. The full-exponent gap
uses the entire geometric sum and applies to every positive n. The two
CE modules already formalize this actual root-gap connection, so the root
does not duplicate it with an unproved sum-bound hypothesis.

EP proves its rational cyclotomic valuation spectrum by divisor induction
from the binomial lifting law. The unique possible bad top-factor prime
is the largest prime divisor of n, of depth one; q=2 is explicitly covered.
The conjugate-root length inequality gives the exact factor Phi_n(1),
whose value is proved by divisor induction. The inert rank-one argument
works for every odd n. The mass constant kappa is retained, and the even
integer rho>=2 plus actual root growth gives the strict 243n/(64kappa)>1
support-existence bound. The root read final EP TeX in full and passes
the transcription. Failure of the totient condition is not a no-go result.

The root read all 32 signed-arm Lean proofs in full. Output primitivity
is explicit in the coprimality theorem; finite lists contain integer
weights, not secretly real logarithms. The root wrote seven support
assembly declarations after their ordinary proofs. Prime-order and
valuation allocation are not asserted in these signatures. A fresh
scoped build independently recompiles all 54 new and 54 unchanged
dependency declarations and audits every theorem's axiom list. Exact
sources and actual successful logs are recorded in lean_validation.json.

The root independently read all three finite verifiers, including the
shared exact factor implementation, and actually replayed them with
--check. The source/certificate hashes are in finite_replay_validation.json.
The 113 actual rows and 678 Fraction inequalities do not prove asymptotic
membership. The 116 and 14 cyclotomic sets, and the 3931 primitive norm
identities, do not assert simultaneous pure-power seed existence.

These are independent internal research-agent reviews, not external
peer review. No full formal proof or disproof of standard ABC is claimed.
