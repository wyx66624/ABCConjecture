# Research continuation after the sixteenth publication

The active goal was actually read as ACTIVE on this continuation. The old
pause snapshot below is historical, not the current application goal state.
The sixteenth ordinary proofs, 27 new Lean declarations, 72 fresh axiom queries
and 544-page manuscript are now sealed; see README.md and verification/.
Non-rewriting publication must still be checked by the actual Git remote,
and remote CI must be read live. No ABC proof or disproof has been found.

The latest local research is deliberately independent of that sealed scope:

1. Critical's seventeenth_round/collective_norm_content.md proves exact
   oriented content identities and the full-block logarithmic height gain;
   both peers reviewed it. complement_determinant_cancellation.md proves
   that multiplying an owner by its common complement and renormalizing the
   three determinants returns the original primitive owner exactly. Thus
   this particular attempt to turn content cancellation into a radical gain
   does not work. collective_primitive_profile.md has a stronger small-lambda
   obstruction based on actual individual prime-depth bounds; recheck its
   final appended theorem and review hashes before formalization.
2. Independent's seventeenth_round/padic_closure_entry.md proves explicit
   rational divisor classes dense in J(H1)(Q5) and globally five-saturated.
   Two peers checked the ordinary formal-log, exact ninefold coordinates,
   group transfer and replay. This supplies the extra closure input for
   practical quadratic Chabauty, not a computed point set. The monic even
   model normalization and isomorphisms are saved separately. A direct
   nonminimal-model PARI height comparison has an unexplained discrepancy;
   isolate it and establish differential/local-height normalization before
   using it in a zero certificate. The ordinary closure proof does not
   depend on those numerical height values.
3. Adversarial's seventeenth_round/actual_divisor_partition.md constructs
   infinitely many actual balanced no-flag endpoints with zero divisor
   partition penalty and unbounded absolute defect. Root and critical read
   the full argument and the inherited FCRT hypotheses. It is not a
   normalized ABC counterfamily. The finite replay distinguishes three
   complete actual inputs from 66 congruence-only checks.

These seventeenth files are local next-round research, not silently included
in this publication or asserted to be fully formalized. Continue their
independent review, prove the next mathematical connections before Lean,
and publish reliable results with their exact scope in the next supplement.

## Historical pause snapshot retained from the fifteenth continuation

ABC remains unproved and undisproved. This directory is a recovery entry,
not a sealed manuscript supplement and not a formal proof. The application
goal tool reported `paused` during the fifteenth publication; that status
was not changed or replaced. The agents completed their bounded work and
saved the following material without starting further batches.

## Published baseline

- main, origin/main and remote main were all actually verified at
  `f2fd471e600fb9d61b6ff10ea7777d50bace3085`.
- The designated PDF has 536 pages and 3,463,794 bytes, SHA-256
  `ad6808a056037d3e3f5ded6f76f7017f9f7dc3207c0ea97443eabece01bda0b0`.
- The stable build used 202 actual TeX sources, with 196 old child sources
  byte-identical, 521 text-prefix pages unchanged and all 16 required rasters
  actually viewed. There were no layout/reference warnings.
- 55 new declarations and 56 unchanged dependencies were freshly compiled
  together; all 111 axiom queries passed with only the standard three axioms.
- The exact replay passed eleven rational-function identities, the quadratic
  unit witness and the 486 primitive mod-27 model hits.
- At the initial remote observation, shared scoped run 34122908285 had
  succeeded; new scoped run 34122908280, kernel 34122908300 and all-module
  34122908283 were still running. A subsequent actual read confirmed that
  34122908280 completed SUCCESS at 12:40:52 UTC, including fresh 55+56
  compilation, all axiom queries and the exact descent replay. The last
  observation still had the kernel and all-module jobs running. Refresh
  before making a current claim about those two jobs.

The authoritative sealed source and scope records are under
`../2026_09_07_signed_moment_descent/`. Do not alter its finalized proofs,
logs or TeX when starting a later round.

## Independent retained routes

1. Critical's `../2026_09_07_critical_bottleneck/sixteenth_round/README.md`
   is the joint-prime entry. Its `projective_packets.md` has three complete
   internal ordinary reviews. The prime-independent quotient by cube roots
   of unity gives a combined-precision rigidity bound with three determinants.
   For a common-hit rectangle, log(product q^e)>=6 nu log(8B) gives the signed
   ball count at most n^(number of primes). This is not a cross-prime saving
   for different singleton owners. The next mathematical target is the actual
   nonrectangular incidence and signed integer resultant/radical ledger.

2. Independent's `../2026_09_07_independent_route/sixteenth_round/RESUME.md`
   records QS1--QS4. Root and both peers completed ordinary review, and two
   independent finite-field implementations checked the complete counts.
   The two remaining genus-two Jacobians are Q-simple and not Q-isogenous;
   an order-three automorphism supplies a Q(sqrt(-3)) action and even ranks.
   The good-five group orders 31 and 19, with good-seven order 84, exclude
   all rational torsion. A nonzero rational point-difference class gives
   positive rank, hence both ranks are at least two. The genus-six Jacobian
   therefore has rank at least six. This excludes only the direct classical
   strict rank-less-than-genus criterion, not point determination or extension
   fields, quadratic Chabauty, higher descent or other methods.

3. `quadratic_chabauty_entry.md` in that same independent directory is an
   ordinary QH candidate, explicitly awaiting peer review. It proposes using
   the already known rank-two first genus-two quotient, whose Jacobian is
   isogenous to E^2, to obtain a finite level-two p-adic container. No such
   container has been computed, no full point set obtained, and no claim about
   the intrinsic level-two locus of the genus-six curve is inferred. Review
   the primary Balakrishnan--Dogra criterion and exact map/domain before use.

4. Adversarial's
   `../2026_09_07_adversarial_audit/sixteenth_round/FLT/package_root_repair_plan.md`
   preserves the local FLT import repair. Nineteen unchanged official sources
   have returned successful compilation, and twelve named solution/export
   queries were audited. The attempted larger batch stopped because a new
   P2M directory shadowed the prerequisite P2M.Util object in a later search
   root. The proposed unified isolated object tree has not been executed.
   Keep the 30-source, 600-second, monitored 8 GiB RSS, single-process lean-only
   bounds and exact source hashes; never silently change an upstream proof.

The sixteenth work has not yet been formally implemented, incorporated in
the designated PDF or published as a completed checkpoint. The QH candidate
has a weaker review status than QS/JP. All unrefuted parent routes stay open.

## Workspace preservation

The user's deletion of `output/pdf/ChatGPT_ABC_Uniformity_2026.pdf`, `.agents/`,
the alternative 412-page PDF and older `next_*` files were left untouched.
The repository also contains the new untracked sixteenth research notes.
Do not stage, restore, delete or overwrite these indiscriminately. Future
publication should use another reviewed source whitelist and non-rewriting
push to main, after mathematical review, formal scope verification and the
complete manuscript render-and-review sequence.
