# Twelfth continuation: independent mathematical and source review

ABC remains unproved and undisproved. This record separates ordinary proofs,
source-level semantic review, fresh compilation and finite replays.

## Ordinary proofs and transcriptions

The root fully read the complete MC, PN, PF, RL, UG and HG ordinary notes.
All received PASS with the stated domains and remaining obligations; details
and independently opened primary inputs are recorded in `ordinary_review.md`.
The three mathematical agents exchanged independent reviews. Their records
are in their respective `twelfth_round` directories. These are internal
independent-agent reviews, not external peer review.

The root fully read all six complete mathematical TeX transcriptions and
the geometry bibliography. The root's finite-count and final-status TeX
were fully reviewed by critical and adversarial. The two integer domains
in the finite-count proposition/corollary were made explicit after review.
Subsequent full-document typography fixes preserve all statements and proofs;
the final source inventory binds the actual built bytes.

Critical, adversarial and independent all fully read the six final root
Lean declarations. They checked the actual nonempty image-fiber choice,
the absence of an unnecessary reduced-fraction premise, positivity and
Nat.abs conversion, finite supremum, complete image partition, fourth-power
threshold and real square-root cast. The hcard and hdiv interfaces stay
explicit. Their source reviews do not claim to repeat the root compiler run.

The root and both peers also fully reviewed the 15 phase declarations.
The root then freshly compiled those two new modules and the unchanged
Eisenstein module together. All 21 new and 29 old declarations pass all
50 explicit axiom queries with only the standard three axioms. The initial
Finset.sup inference failure was corrected before the successful run.
Source hashes and actual compiler output bind the successful evidence.

## Exact finite evidence

The root actually ran all three canonical `--check` replays through
`verify_finite.py`, confirming unchanged source and JSON bytes.

- PF: 2,870 actual block pairs, 1,540 block phase fibers, 1,599 positive
  inputs and 1,201 specified integer phases, including full factorization
  of the two collision constants. The range is not all rational phases.
- PN: 3,522 full norm factorizations, 2,502 private-prime witnesses and
  7,700 ordered pairs/triples on explicitly selected finite private sets.
  The actual norm factorization and permutation fibers are checked. No
  asymptotic density or common deep-prime membership follows by testing.
- RL/UG/HG: six polynomial identities; 21 specified homogeneous branch
  cases including infinity; 614 finite norm-one inverse checks and two
  infinity values; 360 actual first-square positive charts; 50 odd-index
  Laurent and genus arithmetic checks; one V4 group-ring identity.
  The first-square chart checks do not assert a power second norm.

The root fully read the PN and geometry replay sources. Polynomial gcds,
rational inverses and Laurent exponent identities use exact arithmetic.
The finite curve checks do not prove etaleness, Jacobian torsion, ranks or
completeness of rational points. Those general statements have their
separate ordinary proofs or remain open. The wrapper compares the actual
JSON file hash, distinguishing it from any internal canonical payload hash.

## Remaining research and formal scope

The unbounded signed packet, exceptional roots and uniform point heights
are open. The finite-ring torsion construction, lifting, ideal factorization,
asymptotic density, divisor estimates, sieve and geometry are not included
in the 21 formal declarations. Later varying-moment and multiplicative
matroid notes are separate candidates outside this sealed continuation.
Only complete-premise counterexamples retire their exact claims; all
unrefuted parent routes remain active.
