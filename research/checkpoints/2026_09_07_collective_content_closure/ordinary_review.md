# Ordinary mathematical review and publication scope

The ordinary arguments preceded their formalization. Root and the independent
agents reviewed the mathematical sources; final TeX transcriptions were reviewed
separately. These are internal independent reviews, not external peer review.
The exact current source hashes are recorded in
`verification/ordinary_source_inventory.json` and the linked agent reviews.

## Collective content and extraction

CC proves the actual signed oriented content formula and the macroscopic
interval norm estimate, using the stated fixed-progression prime theorem.
CP separates the primitive norm into a squarefree private factor and a capped
small-prime remainder. Every power extraction must leave the former in the
residual factor; one actual prime of the extracted power gives the sharper
exponent ceiling. This is an input-norm statement, not a replacement for the
boundary radical. The interval estimate and its asymptotic quantifiers remain
ordinary mathematics.

CD computes the literal common-product/complement map. Bezout proves its
normalizer divides the norm, and its exact gcd reduction recovers the original
boundary. Root checked that the Lean map derives these facts rather than
assuming its intended equations. Its signed prime sum is interpreted as the
displayed divisor sum only for a positive boundary product. The arbitrary
natural-number Lean definition also covers zero without claiming log-zero
arithmetic. The 24+16 new declarations and 55 unchanged dependencies passed
the recorded fresh compiler and complete 95-query audit before this review.

## New affine outputs

Root read all LM1--LM5, its full TeX, and the complete standard-library replay,
then actually ran the unchanged replay with --check. Exact output:

    70945a8da8fdecee2a8857dd9ea71ab2bb12f9054a8f25b35ad87f342df6d767

The finite-difference vector is a nonzero coefficient vector in the actual
two-coordinate kernel. It refutes only the inference from nonzero coefficients
to nonzero output. Precision e+1 preserves the prescribed depth; the norm-unit
condition prevents removing a selected prime through content. Two determinant
identities give both divisibility of content and the surviving height cost.

For fixed data, freezing every exceptional prime one digit beyond the cubic
valuation also freezes content. Outside that finite set, the three affine
factors have distinct simple roots modulo p. Thus p squared dividing the
product divides one individual linear factor, which supplies the necessary
sqrt(C_0 X) tail cutoff. Finite CRT plus the complete convergent tail proves
the positive density; it is not inferred from the finite sample. The exact
signed identity includes all exceptional primes. The positive-depth example
has exponent two, not the original prime-exponent/far-prime conditions.

Root and both peers noticed and the author corrected a transcription comma
inside an exponent before publication. The power specialization also explicitly
requires positive integer n,B. Neither correction changes the ordinary theorem.
The outputs are generally not powers and the threshold is not uniform in
moving packets. Those bridges to the original tail remain unproved.

## Five-adic closure, height transport, and all other local places

QL gives exact ninefold-point formal parameters and the complete five-adic
closure of the specified rational divisor subgroup. The target group is the
actual product of two different elliptic curves; the classes supply its axes
without identifying the two groups through a spurious matrix. Local closure
and five-saturation do not assert a global Mordell--Weil basis.

HT's differential pullback and the unit-root slope transformation fix the
global height/log-square coefficients on the standard quotient models. The
intrinsic scalar and the version-specific raw PARI vector are different objects.
The exact modulo-25 discrepancy uses integral odd sigma and the bound on its
entire omitted tail, not agreement of two numerical precisions. Root actually
read the primary height construction and pinned source in the preceding work,
and ran the full PARI 2.15.4 replay successfully. The portable CI check separately
recomputes the exact leading-digit proof and checks recorded precision balls;
it does not call PARI or certify those high-precision algorithms in Lean.
The source archive and its selected C files remain local audit material;
the publication provides a versioned official URL and archive hash.

Root read the complete LH ordinary proof and final TeX and actually opened the
author-hosted Cremona Chapter 3, printed page 72, Proposition 3.4.1 and its
preceding paragraphs. They explicitly give twice-Silverman normalization and
applicability to arbitrary Q_l-points. The cases used are (a) and (c): at three,
the actual second point has valuations (1,2), the division-polynomial value has
valuation at least six, and the derivative/c4 conditions hold. The remaining
term cancels the character. At two all three valuation ranges give zero, and
all good primes other than five are handled together. Punctured neighborhoods
handle every projective exception, with nonemptiness from (1,8).

Root also actually opened Balakrishnan--Dogra Theorem 1.4 and Lemma 7.4 and
checked the models, maps, rank-one hypotheses and signs. The two equal scale-two
local constants cancel. The complete away-from-five value set is therefore

    Omega = {-(4/3) log_5(3)}.

This closes the full local-value gate for this fixed curve. It neither solves
the remaining five-adic equation nor classifies all rational points. The
zero fiber is not rational over Q, and translated formulas requiring its
rationality cannot be used. All analytic zeros, their multiplicities, the
rational sieve, common-source inverse conditions and moving-parameter heights
remain open.

Primary sources actually read:

* https://johncremona.github.io/book/fulltext/chapter3.pdf
* https://arxiv.org/pdf/1601.00388
* https://bpb-us-e1.wpmucdn.com/sites.harvard.edu/dist/a/189/files/2023/01/Computation-of-p-Adic-Heights-and-Log-Convergence.pdf
* https://pari.math.u-bordeaux.fr/pub/pari/OLD/2.15/pari-2.15.4.tar.gz

## Actual radical partitions

DP preserves every balance, no-proper-face and actual-divisor hypothesis in its
infinite family. Its absolute defect can be unbounded without giving a fixed-
epsilon ABC counterfamily. LP proves an exact central gap by partitioning all
divisors according to whether they contain the actual large prime. Root read
the complete ordinary proof, final transcription and recursive certificate
verifier, and actually ran --check. Its complete predecessor factorizations and
order witnesses prove primality without a probable-prime oracle. The example
refutes Gamma=0 and Gamma<=D, not an estimate with an arbitrary additive
constant. The proposed infinite prime-value family remains conditional.

## Conclusion of the review

The selected ordinary arguments and their explicit scopes pass this internal
review. The source-specific software evidence, exact finite certificates,
ordinary infinite arguments and newly kernel-checked cores are kept separate.
No all-prime original-boundary estimate, arbitrary-ABC coverage or global proof
or disproof has been established. New next-round candidates are excluded from
this publication until their own proofs and independent reviews are complete.
