# Research continuation after collective content and the local height target

ABC remains unproved and undisproved, and the full application goal stays
active. The preceding status-answer turn made progress by incorporating the
reviewed local-value result into the current gap assessment and by correcting
the finite-evidence scope. This continuation integrates the complete reviewed
proofs, including the exact affine selector and its fixed-data infinite sieve.

## Authoritative publication checks

Read README.md, verification/manuscript_validation.json and the actual Git
remote before identifying the current publication. Local compiler results
prove 40 new declarations and 55 unchanged dependencies in six modules, with
95 axiom queries. Eight finite records have a separate manifest. No claim
about remote CI should be made without inspecting that commit's live runs.

The prior published baseline was main 66d4323bdc90f1283aa0da24900058bd6280f5c1,
with the 544-page PDF hash
aec9b8b9f59fd51d8830abcaaab64dd15f69e0248064d09ea9b042f247cc6cd7.
The four recorded CI jobs for that exact baseline were actually observed
successful before this continuation. They do not verify a later commit.

## Independent next mathematics

1. Arithmetic: the fixed-data affine output theorem pays every selected prime
   contribution but generally leaves the original power family. Prove or
   refute a precisely stated transfer or controlled intersection, retaining
   modulus/content height costs and every moving-parameter dependence. The
   common-complement and full-interval-product shortcuts have exact barriers;
   these do not retire arbitrary mixed, nonlinear or richer-output constructions.
   New candidates belong to critical_bottleneck/eighteenth_round/next_*.
2. Geometry: the complete fixed-curve target is Omega={-(4/3)log_5(3)}.
   Compute compatible five-adic analytic expressions with all-tail bounds on
   every chart, certify all zeros and multiplicities, then apply the rational
   sieve and both common-source square tests. Do not assume the zero-fiber
   point (0,3i) is rational over Q. Preserve global versus local height
   normalization and the trusted software boundary. New work belongs to
   independent_route/eighteenth_round/next_*.
3. Combinatorics: the old actual allocation formula has a precise large-prime
   gap, and the example Gamma>52D rules out two easy strengthenings. Develop
   an actual alternative certificate on that class with no duplicate prime
   charge, or attack the exact uniform arithmetic estimate. The proposed
   infinite prime-value counterfamily is unproved. New work belongs to
   adversarial_audit/eighteenth_round/next_*.

None of these finite/fixed branches covers arbitrary exponents, roots,
nonunit residuals or all primitive ABC triples. Keep the uniform-height,
full signed-ledger and coverage dependencies explicit. Formalize only after
the ordinary theorem and its complete independent review are finished.

## Workspace preservation

Preserve the user's deletion of the alternative old PDF, .agents/, the
alternate 412-page PDF, older next_* probes and the two preexisting unstaged
seventeenth review edits attached to the previous publication. Do not stage
all research files indiscriminately. Local PARI source audit copies and
generated Python caches are outside this publication's whitelist.
