# Collective content, affine compensation and the local height target

Standard ABC remains unproved and undisproved. This continuation integrates
the reviewed seventeenth-round results and the reviewed affine, height and
actual-partition supplements from the eighteenth round. Ordinary proofs came
before Lean. Their internal independent reviews are not external peer review.

The designated manuscript has 568 pages and 3,644,072 bytes, SHA-256
`e713f1761293c12bd8cd46e83d9f4c31c1250c9a8c1e38268317453ad381c55a`.
The stable three-pass build used 217 actual TeX files. All 207 previous child
sources remain byte-identical, and the first 538 extracted-text pages are
unchanged. Root viewed pages 1 and 539--548; two independent agents actually
viewed pages 549--558 and 559--568. These 31 PNG reviews cover the title and
every changed page. No layout or reference warning remains.

## What the mathematics establishes

* Actual oriented collective content has an exact formula. The primitive
  interval norm has a large squarefree private part; every power extraction
  leaves that part in its residual, with the sharp stated linear lower bound
  for its compression cost. This concerns input norms, not boundary radicals.
* The common-product/complement determinant construction recovers the original
  primitive boundary exactly. Its radical and signed ledger are unchanged.
  This rules out that specific attempted free gain.
* An actual affine selector retains different owners' prescribed depths and
  pays an explicit height cost after content cancellation. For fixed data, an
  elementary complete sieve gives a positive-density family whose remaining
  support is squarefree, and hence an exact favorable full signed identity.
  These new outputs generally leave the original power family; their useful
  threshold is not uniform as the packet changes.
* The specified rational divisor subgroup is dense in the full five-adic
  Jacobian group. Exact model/differential transport fixes the height
  coefficients and explains a pinned PARI raw-output discrepancy. An exhaustive
  valuation proof gives the complete away-from-five target
  `Omega={-(4/3) log_5(3)}` for the fixed genus-two curve.
* Actual balanced endpoint families have an exact radical-divisor optimization.
  A fully factored nonsquare endpoint has `0<D<1` but `Gamma>52D`. Its complete
  recursive Lucas certificate uses no probable-prime oracle. This refutes two
  specific strengthenings, not the parent uniform gate or ABC. The proposed
  infinite prime-value continuation remains explicitly conditional.

The full arguments, actual domains, source dependencies and remaining gates
are in `ordinary_review.md` and the integrated manuscript sections. Final TeX
sources and ordinary sources have separate byte inventories and reviews.

## Exact formal and computational scope

`Lean/ComplementContent.lean` supplies 24 new declarations and
`Lean/SquarefreePowerExtraction.lean` supplies 16. All 40 and 55 unchanged
dependencies were freshly compiled together in six source modules, with
warnings treated as errors and all 95 explicit axiom queries checked. Only
`propext`, `Classical.choice` and `Quot.sound` occur. Lean is 4.32.0 and Mathlib
is pinned to `81a5d257c8e410db227a6665ed08f64fea08e997`; its compiled cache was
reused. See `verification/mathlib_validation.json` and the complete build log.

Actual gcd, primitive division, the complement map, its radical observable,
natural squarefree divisibility, prime-depth and logarithmic consequences are
formal. Distribution, infinite sieve, p-adic height theory, Jacobian closure,
the local-value proof and rational-point completeness are not made formal by
the finite statements or the verifier names.

Eight finite replay records cover CC, CD, CP, QL, DP, LP, HT and LM. The portable
HT check recomputes the exact leading-digit argument and checks algebra on
recorded precision balls. It does not recompute PARI heights. The separate
full PARI 2.15.4 replay was actually run locally; its high-precision algorithms
remain trusted computational inputs, distinct from the ordinary all-tail
proof. The versioned official archive and source hashes provide provenance;
third-party source copies are not included in this publication.

From the repository root, with the pinned Lean project available:

```text
python -X utf8 research/checkpoints/2026_09_07_collective_content_closure/verify_mathlib.py
python -X utf8 research/checkpoints/2026_09_07_collective_content_closure/verify_finite.py
python -X utf8 research/checkpoints/2026_09_07_collective_content_closure/verify_ci_failure_paths.py
```

Three actual negative-path checks verify that missing dependencies or a
missing certificate cannot preserve a historical PASS. CI invalidates old
outputs before dependency work, and each standalone verifier invalidates its
own output before preflight. The new Linux workflow separately rebuilds the
listed source modules. Its result must be read live for the exact commit;
local PASS does not establish an unobserved remote run.

## Manuscript and open continuation

The designated filename remains `output/pdf/ChatGPT_ABC_Uniformity_2026_08_31.pdf`.
Its final PDF, stable TeX inventory, unchanged previous sources, changed-page
visual reviews and exact digest are bound by
`verification/manuscript_validation.json`. Compilation alone is not a visual
review. The checkpoint does not assert that every historical page was rerendered
or that the entire manuscript has been formalized.

The original all-prime signed tail, exceptional roots, independent-domain
complement, moving residuals and arbitrary ABC coverage remain open. On the
fixed curve the local target is now known, but all five-adic zeros, their
multiplicities, the rational sieve and common-source inverse conditions remain
to be proved. Uniform heights over moving curves are an additional global
obligation. All unrefuted parent routes remain available; `next_*` candidates
are excluded from this sealed mathematical scope.
