# Unit-normalized signed moments: full ordinary review

Reviewer: adversarial_audit. US1--US4 complete ordinary review: PASS. The source was actually read in full. This is next-only work after the frozen fourteenth-round release.

The three boundary phases in US2 are correct: C=0 gives one, A=0 gives zeta^2, A+C=0 gives zeta^4. Their unit normalization uses gcd(n,3)=1 and is fixed across precisions for each actual arm hit. Cubing any relation eliminates all cube-root twists and recovers multiplicative independence; the normalization need not be the same for different q. The image of v/bar(v) on the six units is exactly mu_3, so every signed ratio product has the asserted integral numerator made from unit-rotated roots or their conjugates.

The exact Gram determinant inequality handles arbitrary unit rotations, conjugation, different lengths and the empty product uniformly. The ratio 9/(8sqrt(3)) is strictly less than one. Unit denominators and q>3 justify integer determinant divisibility; the precision bound forces exact equality. Independence recovers the integer signed exponent vector. There is no fictitious independent unit or duplicate treatment of cancelling signs. The n-torsion cardinality is valid for split and inert residue algebras because q does not divide n.

The integer l1-ball formula and all three special cases are correct, including M=0. Its order-two and radius-r inequalities give M4<=sqrt(n/2) and Mh<=1 for r=ceil(sqrt(n/2)). The bound r<=sqrt(n), full middle layers, one whole-arm cap and normalization yield 5/B. The radius-three bound, complete two-range remainder and the conservative 7/14 constants are valid. Both Brun--Titchmarsh constants and the explicit unknown far-label cardinality interface are preserved.

The depth-four singleton region and the full-support singleton region have different thresholds, exactly as stated. The latter needs log q>=2rL, so the manuscript does not mistakenly exclude depth-one/two support at the weaker threshold. The statement cannot forbid credits at other primes or outside I, and gives no total label or owner-cost bound. No new analytic input, Lean statement, actual-prime distribution, or global signed conclusion is being inferred.

Source SHA256: fcc3a221207be2e5a21eb98b24a4e8a7872189a60f3eedb1a3dca0ac6388c910
