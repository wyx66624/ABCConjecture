# Actual diamond/high-depth bridge: pre-formal scope review

Reviewer: adversarial_audit. Status: PASS after actual full reading. No compiler execution or future source proof is claimed by this record.

For each distinct i,j, restriction of the actual private integer-valued group homomorphisms gives a two-coordinate diagonal matrix with nonzero diagonal and zero off-diagonal entries. Its diagonal integers may have either sign; cancellation in the integers remains valid. The stated signed-product rigidity, together with that private-valuation argument, is exactly the premise needed for the existing concrete diamond injection. Therefore its cardinality 2r^2+2r+1 is at most |H|, at most n, and at most 2r^2. The contradiction does not need an extra r>0 assumption; at r=0 the stipulated finite group size is itself impossible.

The same proof applies to the actual high-depth filtered subset. The distinct elements, private maps, product equality and image group must be restricted to that subset, while the lower-depth count and individual depth caps remain conditions on the original finite set. With this distinction retained in the implementation, the final critical theorem yields the entire natural-truncated excess, not merely the contribution above the adaptive threshold.

The addendum correctly retains the low-layer cardinality inequality, finite target-size bound and modular rigidity as explicit inputs. It neither assumes the desired high-filter cardinality nor constructs an arithmetic torsion group, but also does not claim the latter construction or any all-prime sum. This is a sound bounded extension for implementation.

Source SHA256: 2ab91f9f9cc4dc9d6d53b1efe06bb55ec6a1b039519f826fb383fac176445d78
