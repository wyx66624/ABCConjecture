# Signed moment arithmetic: final source review

Reviewer: adversarial_audit. **PASS after actual full reading of all 22 new
theorem statements and proofs**, the `ballCount` definition, and the complete
recorded 49-query log. This is an independent semantic review and evidence
cross-check, not an independently rerun compiler invocation.

Final source SHA256:
`01d03813915c7fcffcf2994cfde948a0d961ac5b6dcc69af77fb05beb5c65327`.
The exact source, manifest and log bindings are in
`verification/signed_moment_source_review.json`. All three listed source
hashes were actually recomputed. The 22 new plus 27 old names match the
complete log exactly, without duplicate or missing queries; its union is
only the standard three axioms. The existing dependent module proofs are
the previously reviewed, unchanged bytes.

The binomial quantity is a literal finite natural sum. Its special-value
identities include the small-coordinate cases, and the monotonicity and
numeric consequences are proved. The natural radius treats odd n and
perfect-square half-values correctly; its upper bound explicitly needs
n at least 18. `count_le_one_of_actual_diamond` retains the actual diamond
cardinality implication as a premise instead of hiding a field argument.

The owner is genuinely chosen from the actual finite depth function, with
empty sets and ties covered. The one-deep-index argument proves that this
maximum contains the deep filter. The complete remaining excess reuses
the actual finite two-range inequality at lower threshold four, then
removes at most one whole individual cap. Both every summand and the total
sum are natural truncated differences before being cast to the reals.
The square inequality gives the exact 5/2 bound, and the final natural
radius corollary connects the proven count consequences to that bound.

The two-range theorem retains the whole intermediate range and the
automatically selected single owner. It proves the explicit finite
coefficient `6 M4 + 2 r Mh0`; the later fractional-power 7/14 simplification
is not claimed here. The normalized theorem uses actual per-index heights
bounded below by a positive `t0`, not a supplied averaged conclusion. The
last 10-to-11 progression comparison includes the necessary real n at
least 11 and positive denominator assumptions.

The final finite budget still explicitly assumes the signed-count bounds
and the individual full-depth cap. It does not construct residue torsion,
Gram rigidity, prime valuations, prime distribution, or domain density.
In particular, it supplies no all-prime label bound, no automatic
membership of the independent domain, and no global ABC conclusion.
