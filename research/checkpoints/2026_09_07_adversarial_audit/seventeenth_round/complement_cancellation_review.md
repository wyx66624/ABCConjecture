# Independent ordinary review of CD1--CD3

Status: full ordinary PASS. Source:
critical_bottleneck/seventeenth_round/complement_determinant_cancellation.md.

The multiplication matrix of V has determinant N(V), commutes with the
rotation R, and sends 1,zeta,zeta^2 to V,R(V),R^2(V). Its three determinant
identities therefore give exactly cd(x,D,y)=N(V)(-b,a,a+b), with the signs
and ordering in the source. Since W is primitive, an actual integer Bezout
identity ua+vb=1 implies N(V)=cd(uD-vx). Thus r=N(V)/(cd) is an integer;
its positivity follows from V nonzero and c,d positive. Cancelling cd
proves the three equalities and gcd r. Zero individual coordinates of W
do not invalidate the proof; W=0 is already excluded by primitivity.

When ab(a+b) is nonzero, normalization by r recovers exactly the primitive
boundary (-b,a,a+b). The absolute product, radical and full signed
valuation ledger consequently agree with those of W. No repeated prime
from r has been counted as independent radical credit. The all-owner
application keeps each complement distinct and uses the already established
primitive unramified power property of each actual W=w_k^n; the empty
complement is allowed.

The conclusion obstructs only the proposed common-multiplier/complement
determinant map. It does not rule out mixed maps, relations between
different primitive outputs, or the entire collective-content and
cross-prime programme. The stated finite formal core is sufficient for
this exact arithmetic statement and requires no PNT, torsion or ABC input.
