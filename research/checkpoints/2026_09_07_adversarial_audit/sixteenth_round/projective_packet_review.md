# JP1--JP3 independent ordinary review

Status: FULL ORDINARY PASS.
Reviewed source SHA256: `9a614c79b25c92c3c4cd56a6877ddcfcfcdb7734b56075d25db602c842a094f7`.

The setup keeps n>324 prime, B=n^4 and an actually multiplicatively independent subset of the root block. Passing from the norm-one group to its quotient by mu3 preserves independence: cubing a quotient relation is an honest integer-exponent relation in the original group. Reduction is used only on the subgroup of ratios integral and invertible at every marked prime, not asserted on the entire global rational group. Marked roots satisfy this necessary unit condition.

For q>8B, q differs from n and three. The finite norm-one quotient by mu3 has a q-group reduction kernel; multiplication by n on that kernel is invertible. Its n-torsion consequently injects into the cyclic precision-one quotient and has size at most n. All primes in a packet have separate finite targets.

The combined-precision proof handles signs, unequal lengths and empty products. For each pair of signed vectors and each prime there is a unique local mu3 phase. Choosing all three global unit representatives gives three actual integer determinants. Each marked prime power divides its own phase determinant; distinctness of the primes gives divisibility of the product of determinants by the entire product modulus. Each determinant has absolute value strictly below (8B)^(2nu), including the empty-product cases, by the actual Gram bound. Under log m >= 6 nu L the integer product must vanish. A zero determinant yields equality modulo a global unit phase, and the original actual independence forces equal signed vectors. No common local phase is assumed.

The signed-ball count is therefore bounded by n^s, with s the number of marked primes. The whole argument is for a rectangular common-hit subset J: every root in J meets every marked prime at the specified precision. It neither counts arbitrary nonrectangular incidences nor bounds singleton private labels. The n^s target cannot be replaced by n using only its exponent.

As an interpretation, cubing identifies G/mu3 with its image G^3, so the factor-three precision charge may also be viewed as the length cost of fixed cubed representatives. This is a valid joint phase/precision ledger, not an additional single-owner saving or a bound on total far-tail cost. No new finite replay, primary source read or Lean compilation is claimed for this ordinary review; the already audited US finite-ring and actual Gram inputs are used.
