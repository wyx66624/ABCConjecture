# Resume after the fixed-curve closure

The previous base was main commit
455c68d6f367073fd0ba5fa59be900eb824334ff. Publication of this checkpoint
adds the 600-page designated manuscript and the evidence described in
README.md. Consult Git and the exact revision's workflow runs for live
publication state; a local validation manifest is not remote CI evidence.

Completed mathematical milestone: C(Q) has exactly the six displayed
points; the actual inverse and same-source second square exclude the
positive D_(3,zeta) branch and its same-unit odd multiples of three.
All ordinary source and transcription reviews are internal independent
reviews. The full analytic chain is not formalized in Lean.

The 24 new Lean theorems are literal nonlinear square/cube coordinates,
norms, boundaries, positivity, explicit Bezout/gcd and boundary-prime
norm-unit facts. They passed a fresh canonical compiler run with 24 axiom
queries and only the three standard axioms. Keep this count separate from
the previous release's 40 new and 55 dependency declarations.

Active, separate next directions:

1. Critical arithmetic: shifted boundary polynomials as coprime
   resultant certificates on original n^4 blocks. Review complete
   cyclotomic root orbits, signed common-depth accounting, the cost of
   multiple shifts, and unmatched excess. A bound for paired depths does
   not cover ultra-large singleton primes or give pointwise ABC.
2. Independent geometry: the remaining zeta-squared unit has a different
   genus-two model and order-three quotient. Verify its finite counts,
   torsion and any actual rank upper bound independently. Do not borrow
   the completed curve's rank or height calculation.
3. Formal audit: formalize the already proved finite arithmetic at
   thirteen in a separate module and checkpoint. A polynomial Bezout
   identity must be a coefficient identity, not merely 13 evaluations.
   Arithmetic predicates must not be represented as abstract elliptic
   multiplication without proving that connection.
4. Preserve divisor-packet supply and moving-data nonlinear thresholds as
   distinct open directions. Record a counterexample to a specific claim
   with its valid replacement rather than discard the parent route.

The principal ABC obligation is a uniform bound on the full signed
valuation ledger, retaining depth-one/two negative contributions, together
with exceptional-root, arbitrary-root, residual and ramification coverage.
The completed fixed branch supplies none of these uniformly by itself.

Do not edit old sealed inventories or unrelated worktree changes. Stage
only a reviewed batch's explicit file inventory and publish without
rewriting main history. Recheck remote divergence immediately before
publication. ABC is still an active research goal, not a completed result.
