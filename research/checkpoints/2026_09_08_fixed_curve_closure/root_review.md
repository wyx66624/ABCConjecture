# Root review of the fixed-curve closure and new arithmetic

This is an internal mathematical review, not external peer review. Ordinary
proof, exact finite computation, and Lean checking are separate scopes.
The source inventory accompanying this release binds the final reviewed bytes.

The root read RD, DS and IF in full and authored their ordinary proofs.
For the fixed rational-locus theorem SM it checked the entire local zero
inventory, the finite-index logarithm bridge, all exceptional reduction
charts and the sixteen-point obstruction at thirteen. Independent agents
separately reviewed the complete combined proof, not just its finite table.

The root reviewed ZS and UD in full, including their external primary inputs,
and independently read and ran their complete exact replay programs. It then
read the full SU and ZD ordinary certificates and their complete verifier
sources. The restricted-series estimates apply to the entire residue disk;
SU's only candidate subdisk has f/5 = 15 modulo 25, and ZD's central
subdisk has f/5 = 20 modulo 25. Taylor integrality and exact parity justify
the exclusions. These are not inferences from sampling centers alone.
Root independently ran SU, ZD, IF and SM --check after reading the programs:
all returned PASS against their canonical certificates. The root also ran
the full CG finite replay: three original prime indices, six actual powers,
literal polynomial contents and valuations all passed.

The CB inverse was read in full against the actual RL rational-domain and
HG conic inverse definitions, including the smooth projective continuation
at t = -1/2, and the CL power-map composition. The six H points give twelve
D points with the second square tested at the same source. All are boundary
points with r = 0; none has finite t > 1. The result propagates within the
same unit class to odd exponents divisible by three. No positive-domain
identification of the distinct zeta-squared class was used. PASS.

For CG, the root read the full original candidate. Adversarial review caught
the missing prime-three adjustment in CG6: T_n/3 does not preserve its
full depth at three. The corrected proof explicitly uses e_3 = 1 + v_3(k),
adjusts the F-packet depth by one, and treats both radical possibilities.
Root then read the whole correction and the exact signed resultant inequality.
The main actual short-vector/index counterexample and CG1--CG5 are unchanged.
The corrected ordinary statement passes; the second polynomial is still
an unproved sufficient construction for the target uniform estimate.

The root read the final PA proof in full, including the corrected R = Q/q
and the m+1 monotone chain. Each prime is allocated once. At most m exact
updates find a central divisor or certified bracketing pair. The supply
of those paired packets and the small scalar defect remain explicit
unproved hypotheses in a uniform theorem. This does not turn the finite
example into an ABC proof. The NP, CU, AI and NG reviews were completed
earlier in the same research run and are separately source-bound records.

Root also read every statement and proof in NonlinearPowerSelectors.lean,
the prior formal scope, the full verifier, and the successful manifest and
complete compiler log. It independently ran a second fresh WSL compilation
on 2026-09-08, recorded at nonlinear-20260908T051433674814Z. All 24 theorem
axiom queries passed with only propext, Classical.choice and Quot.sound.
The compiled source SHA256 is
38849679f2e456475bf9f9ef8dfaa0c55df2e0cba8d705d7ec579c2e111776e9.
Only that new module was compiled against the pinned Mathlib cache; this
run did not compile the complete analytic rational-point theorem.

ABC remains unproved and undisproved. The closure of this fixed geometric
branch and the 24 elementary formal results do not establish a varying
family height bound or a signed boundary estimate for all primitive triples.
