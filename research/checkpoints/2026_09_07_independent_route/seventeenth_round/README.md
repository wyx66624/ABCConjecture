# Seventeenth geometry: a certified local input for quadratic Chabauty

This directory is separate from the sixteenth publication. It contains
no new Lean source, PDF edit, or git publication.

The complete publication transcription is now
`paper/padic_closure.tex`, SHA256
`fc4da850fa5b0de5a9aa9a7bd4ef23c9aeabd8beecbfeb41893253ad27b2e630`.
Both peers actually read the full TeX and gave final transcription PASS.
The mathematical source is frozen. The historical next-only model
normalization probe is excluded from the publication inventory. Subsequent
global-height transport and complete away-from-five local values are
separate eighteenth-round results and are not retroactively claimed by QL.

`padic_closure_entry.md` is the complete QL1--QL4 ordinary proof. Both
critical_bottleneck and adversarial_audit read the entire proof and the
Milne/BD primary inputs and approved it. Both independently ran the exact
certificate. A statement about an integer matrix on two different elliptic
groups was corrected during review: the final argument uses the actual
generators `(0,2P')` and `(-2P,0)` separately. No theorem changed.

The new result is stronger than an abstract finite-index closure claim:
the cyclic groups generated by the two known elliptic points are dense
in their entire Q5 point groups. The two explicit rational divisor classes
`[(0,1)-infinity+]` and `[infinity--infinity+]` generate a dense subgroup
of `Jac(H1)(Q5)` and a finite-index, 5-saturated subgroup over Q. Rational
torsion vanishes. A global Mordell--Weil basis is still not asserted.

Exact reproduction:

    python replay_padic_entry.py --check

The canonical finite arithmetic certificate is
`verification/padic_entry_exact.json`, SHA256
`2bc0673769cc10abba959725e1683676d162918afa7fbf89a463f664c9b24696`.
It includes both complete mod-5 elliptic tables, exact ninefold coordinates,
valuation and unit tests, the H1 F5/F25 counts and the divisor-log matrix.

The separate PARI pilot is runnable using existing WSL PARI/GP 2.15.4:

    python replay_padic_pilot.py --check

It records exact p-adic ball representations at requested precisions 12
and 24, rejects nonempty stderr and failed/missing output, and verifies
agreement at common precision. Its canonical SHA256 is
`3e3acd4aad317504bd611eb7ecfd377107d34c1b7d87e45fcd68b6fffd27c636`.
The two logarithms have valuation 1, the two PARI canonical heights have
valuation 1, and their height/log-square ratios have valuation -1.
Logarithm and height identities predicted by the three-isogeny agree to
the reported precision. Bounded saturation at bound 11 returns the same
points; this is not a proof of saturation at every prime. The ordinary
5-saturation theorem does not depend on PARI's height or saturation code.

Both wrappers were run in write mode and subsequently in check mode.
The pilot is software-assisted numerical evidence with explicit p-adic
precision, not an independent proof of a complete QC zero calculation.
In particular its normalization has not yet been matched to all BD local
height terms. No finite local-height set or complete residue-disk zero
list has been computed.

The next concrete calculation is to fix the monic even model and the
elliptic coordinate/differential normalizations, calculate the bad-prime
local-height value sets, then certify analytic expansions and all zeros.
The global rational sieve needs its own saturation evidence at the sieve
primes. The same-source second square and positive-real condition on D
must be applied afterwards. Varying exponents, residuals, content and
uniform point-height bounds remain parent open problems.
