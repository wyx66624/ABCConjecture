# Independent ordinary review of CC and CD

Full ordinary review: PASS for both files in the critical_bottleneck
seventeenth-round directory. No computation, compiler run, or manuscript
integration is asserted by this review.

CC1--4, `collective_norm_content.md`, SHA256
181d826a9c5ebd89b7faf0ddf05afd5971d600db09be2c9b2a9094923ed73d0b:

The exact signed orientation identities use the two actual ideal depths,
not a common-hit surrogate. Content is their minimum and the primitive
norm exponent is their absolute difference. The canonical signed product
excludes putting both orientations of the same root into a single factor,
which is necessary for the support statement. A repeated norm prime at
distinct roots is at most 12B-5 by the displayed quadratic difference.
The full-interval valuation approximation includes the geometric tail;
the sum of depth errors weighted by log p is O(B). The use of fixed
modulus 3 gives the stated main term with O(H log log B+B), uniformly
in the location and length of the interval. I actually reopened the
primary BMOR arXiv:1802.00085, Theorem 1.2 / printed page 5; its explicit
fixed-modulus theta bound supports exactly the required input. The
deletion divisibility follows from min(a+c,b+d)<=min(a,b)+max(c,d).
The upper bound needed with o(B) deletions also follows immediately from
content(W_J) dividing content(W_full). No moving-modulus or independence
claim is hidden in the asymptotic statement. Input-norm content is not
identified with boundary radical credit.

CD1--3, `complement_determinant_cancellation.md`, SHA256
2cd1583877c359c110c2c4baec3fd62f421ac97b552789cf70840197e397b54e:

The multiplication matrix has determinant N(V) and commutes with the
unit rotation. Each cleared determinant equality follows with the stated
sign. The primitive Bezout identity ua+vb=1 gives N(V)=cd(uD-vx),
which supplies actual integral divisibility rather than merely rational
proportionality. The factor r=N(V)/(cd) is positive and is exactly the
gcd of the three phase determinants, including zero individual owner
coordinates. The normalized additive triple and complete boundary
valuation ledger are literally the owner's original triple. The
full-product/complement application preserves those hypotheses, including
the empty complement, and therefore does not buy extra radical saving
from the cancelled common factors. This audits that specific map and
does not rule out other aggregate constructions or the parent programme.
