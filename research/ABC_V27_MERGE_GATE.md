# v27 merge gate

The v27 affine-residual increment may be merged only after the exact branch
head has passed both repository workflows:

- Lean kernel build;
- Lean all-module audit.

The increment is an unconditional parametrization, primitive-family, and
discriminant theorem.  It does not claim the pointwise residual-radical bound
and does not close `ABCConjecture`.
