\section{Eighth continuation: global image and actual pure-power support}

Standard ABC remains unproved and undisproved. A global projective-image
argument now excludes every CM residual candidate for the actual positive
Frey seeds at prime exponents greater than seven. Actual unbounded local
shadows of a CM boundary remain valid; they pass the specified finite
tests but do not satisfy the asserted global representation comparison.

The two remaining fixed-level non-CM orbits are exact quadratic twists.
Two independent computations compare every coefficient through the proved
common-level Sturm bound, including bad-prime indices. This establishes a
common boundary module for every actual pure prime power, retaining all
coefficient embeddings and character twists. The boundary's rational
two-torsion then excludes the exponent prime from the norm support and
gives the stronger necessary cutoff $q\ge(\sqrt{2p}-1)^2>p$.
It yields a height lower bound and an effective exponent budget, without
a uniform upper bound for actual points.

The analytic route separately extends its root-window average to every
exponent index. Primes above the growing endpoint and prescribed exceptional
roots remain open. Ten new Lean declarations verify the elementary trace
and actual-height consequences with their modular antecedents explicit.
The global modular, local representation and analytic proofs retain their
stated external inputs and ordinary independent review status.
