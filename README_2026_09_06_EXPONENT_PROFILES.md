# Exponent-profile continuation and large-prime multiplicity

**Status: partial research, not a proof or disproof of standard ABC.**

The new [checkpoint](research/checkpoints/2026_09_06_exponent_profiles/README.md)
contains exponent-block logarithmic bounds, an unbalanced norm-power exclusion,
restricted uniform ABC classes without raw norm-prime cutoffs, and an infinite
Linnik family refuting unrestricted raw large-prime excess smallness while R>c.
It records exactly which statements are ordinary proofs and which narrower
integer-pair regrouping and progression statements have passed Lean.

The [master paper](output/pdf/ChatGPT_ABC_Uniformity_2026.pdf) is rebuilt from the
existing LaTeX source plus the new supplement. The baseline mathematics is
preserved exactly after removal of the two explicitly marked integration blocks.
The [build record](research/checkpoints/2026_09_06_exponent_profiles/verification/build_validation.json)
reports the actual PDF hash, page count, input commit and scoped test run.

The original IUT, FCRT, Pell/Mersenne and geometric global gaps remain open.
No independent autonomous-agent execution, full-repository build or external
peer review is claimed. The new global correlated estimate is not silently
assumed or introduced as a Lean axiom.
